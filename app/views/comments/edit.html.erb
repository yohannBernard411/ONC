<%= simple_form_for [@comment] do |f| %>
  <%= f.input :title %>
  <%= f.input :content %>
  <%= f.input :note %>
  <%= f.submit "Submit comment", class: "btnlink" %> | <%= link_to "return", clothe_path(@comment.clothe), class: "btnlink"  %>
<% end %>